# ✅ Correct JDBC URL with dialect, host, port, and DB name
spring.datasource.url=jdbc:postgresql://global-edge-academy-prod.cvkcge6euluh.ap-south-1.rds.amazonaws.com:5432/global_edge_academy_prod

# ✅ Credentials from environment variables or secrets
spring.datasource.username=${PROD_DB_USER}
spring.datasource.password=${PROD_DB_PASSWORD}

# ✅ PostgresSQL driver
spring.datasource.driver-class-name=org.postgresql.Driver

# ✅ JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# ✅ App server port
server.port=8080